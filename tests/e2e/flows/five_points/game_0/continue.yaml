# Debug flow: Continue from handicap adjustment
# Use this to test handicap override entry without running the full flow
# Expects: Game settings screen with course/tee already selected

appId: golf.spicy
# Allow Maestro to see elements inside dropdown modals on iOS
platform:
  ios:
    snapshotKeyHonorModalViews: false
---
- runFlow: "adjust_handicaps.yaml"
