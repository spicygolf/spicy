import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  stat_label: {
    fontSize: 11,
    textAlign: "center",
  },
  stat_value: {
    fontSize: 16,
    fontWeight: "bold",
    textAlign: "center",
  },
  stat_view: {
    paddingHorizontal: 5,
  },
});
