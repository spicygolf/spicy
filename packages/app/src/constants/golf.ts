/**
 * Golf-related constants used throughout the app
 */

/** Default par value when hole data is missing */
export const DEFAULT_PAR = 4;

/** Default yards value when hole data is missing */
export const DEFAULT_YARDS = 0;

/** Default handicap allocation when hole data is missing (18 = easiest hole) */
export const DEFAULT_HANDICAP_ALLOCATION = 18;
