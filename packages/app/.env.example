# API host for development (your local machine's IP for device testing)
# Leave empty to use default localhost
DEV_API_HOST=

# Jazz cloud connection (required)
# Get these from your Jazz dashboard at https://jazz.tools
# JAZZ_API_KEY: Your project's API key (starts with a base64 string)
# JAZZ_WORKER_ACCOUNT: The worker account ID (starts with co_)
JAZZ_API_KEY=
JAZZ_WORKER_ACCOUNT=

# PostHog analytics configuration (optional - app works without these)
# Get these from your PostHog dashboard at https://posthog.com
# If not set, analytics are disabled silently in dev, with a warning in production
POSTHOG_API_KEY=
POSTHOG_HOST=
