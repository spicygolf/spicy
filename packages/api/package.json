{
  "name": "api",
  "version": "0.5.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "generate": "bunx --bun @better-auth/cli generate",
    "migrate": "bunx --bun @better-auth/cli migrate",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format .",
    "format:fix": "biome format --write ."
  },
  "dependencies": {
    "@elysiajs/cors": "^1.3.3",
    "better-auth": "^1.2.12",
    "elysia": "1.3.5",
    "ghin": "github:boorad/ghin#feat/approved-api-access",
    "jazz-betterauth-server-plugin": "catalog:jazz",
    "spicylib": "workspace:*"
  },
  "devDependencies": {
    "@better-auth/cli": "^1.2.12"
  },
  "module": "src/index.js"
}
