import {
  StyleSheet,
} from 'react-native';

export const styles = StyleSheet.create({
  stat_view: {
    paddingHorizontal: 5,
  },
  stat_value: {
    textAlign: 'center',
    fontWeight: 'bold',
    fontSize: 16,
  },
  stat_label: {
    textAlign: 'center',
    fontSize: 11,
  },
});
